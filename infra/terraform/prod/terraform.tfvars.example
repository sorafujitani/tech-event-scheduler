# Copy to terraform.tfvars (gitignored) and fill in real values.
# See variables.tf for descriptions.

cloudflare_account_id = "0123456789abcdef0123456789abcdef"

# --- Custom domain settings (omit if you only need *.workers.dev URLs) ---
# domain  = "tech-event-scheduler.example.com"
# zone_id = "abcdef0123456789abcdef0123456789"

# Stage 1 (initial apply, creates D1 only):
#   attach_custom_domain = false   # default
# Stage 2 (after `wrangler deploy` for both api and web):
#   attach_custom_domain = true    # then `terraform apply` again

# environment = "production"  # default
